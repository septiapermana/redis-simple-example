pipeline {
    agent any

    stages {
        stage('Set') {
            steps {
                sh 'node -v'
            }
        }
        stage('Cloning') {
            steps {
                echo 'Hello World'
            }
        }
    }
}
